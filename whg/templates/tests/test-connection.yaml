apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "whg.fullname" . }}-test-connection"
  labels:
    {{- include "whg.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "whg.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
