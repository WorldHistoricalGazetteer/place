name: Build and Deploy Documentation to GitHub Pages

on:
  push:
    branches: ["main"]  # Trigger the build on pushes to the main branch
  workflow_dispatch:  # Allow manual triggers from the GitHub Actions tab

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      # Step 1: Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: Set up Python (for Sphinx build)
      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: 3.9

      # Step 3: Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r docs-source/requirements.txt

      # Step 4: Build the documentation
      - name: Build documentation
        run: |
          cd docs-source  # Navigate to the docs-source directory
          make clean      # Clean any previous build files
          make html       # Generate the HTML documentation

      # Step 5: Prepare the docs for deployment
      - name: Setup Pages
        uses: actions/configure-pages@v5

      # Step 6: Upload the built HTML files as an artifact
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs-source/_build/html  # Upload the built HTML files

      # Step 7: Deploy the built docs to GitHub Pages
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
